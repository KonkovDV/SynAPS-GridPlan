# Заявка: SynAPS-GridPlan

Пакет для конкурса «Марафон инноваций. Энергия будущего» (МИК, i.moscow,
приём до 20 августа 2026). Репозиторий: https://github.com/KonkovDV/SynAPS-GridPlan

## Задача

Собрать месячный график ТОиР так, чтобы бригада, квалификация, окно отключения,
ЗИП и уже согласованные заявки ПЛ не конфликтовали. Ошибку «работа вне окна»
или «сломана последовательность отключение → ремонт → включение» нужно ловить
в расчёте, а не на объекте.

Продукт — вычислительное ядро. Его встраивают в существующую АСУ ТОиР.
SCADA, GIS и 1С не подменяются.

## Как устроено

На вход: активы, работы, бригады, окна, склад. На выход: график и протокол
проверки. Если проверка находит жёсткое нарушение, план не выдаётся как
допустимый. Склад в этой версии — наличие позиции (одна единица на работу,
не BOM-количество). Техпоследовательность компилируется только как линейная
цепочка: ветвления «несколько предшественников / несколько последователей»
адаптер в движок не передаёт; чекер это ловит, если ребро потеряно.

Поиск слотов — SynAPS (эвристика GREED, при необходимости CP-SAT).
Правила ТОиР проверяются отдельно, на Python; тот же контур есть на Rust
(FIFO и проверка, без GREED).

## Готовность

ISO 16290, **уровень 4**: лабораторные (синтетические) постановки, автотесты,
воспроизводимый отчёт. Порог МИК — TRL 4+.

Уровня 6 нет: нет пилота на объекте и нет промышленных выгрузок ДЗО.

Для перехода на 5–6 нужны журнал заявок ПЛ, справочник бригад и ЗИП,
регламент заморозки. Учёт заказчика не ломается. Грант МИК (до 4 млн ₽)
— на испытания с данными заказчика, не на продажу лабораторных цифр.

## Заказчик цикла

Программа запущена совместно с Группой «Россети» (30.07.2026). Эта заявка
отвечает на запрос **автоматизации процессов** сетевого комплекса: месячный
график ремонтной кампании (бригады × окна ПЛ × ЗИП × заморозка согласованных
строк). Предлагаемый пилот — один РЭС / производственное отделение ДЗО,
3–6 месяцев, П0 ретро → П1 тень → П2 участок. Живых выгрузок ДЗО в
репозитории нет.

ЭЛ5-Энерго и СберСити — тираж той же модели, не второй центр заявки.

## Команда

Публичные репозитории SynAPS-GridPlan и SynAPS — один автор,
[Коньков Д.В.](https://github.com/KonkovDV). Предметного эксперта ТОиР в
этом пакете нет: консультационного соглашения в материалах нет, ФИО не
выдумываем. На пилоте интеграция — куратор ДЗО и файловый контур CSV/JSON.
Штатного интегратора 1С/EAM на дату пакета нет. ИНН / ОГРНИП и статус в
Реестре стартапов i.moscow — только в кабинете анкеты, не в git.

## Рынок (bottom-up, не «рынок ПО РФ»)

Чек разового контура: ₽8–25 млн (лицензия + интеграция + год SLA на
филиал/ДЗО). Пилот отдельно: грант до ₽4 млн на испытания (410-ПП).

| Слой | Счёт | Ориентир |
| --- | --- | --- |
| TAM | 17 контролируемых электросетевых ДЗО ([меморандум](https://www.rosseti-sz.ru/investors/memorandum/)) + крупные генкомпании с собственной ремонтной кампанией | порядка ₽0,26–0,80 млрд / 5 лет, один контур на компанию |
| SAM (3 года) | 6–10 из 17 ДЗО, где цифровизация ТОиР уже в повестке | ₽48–200 млн |
| SOM (12 мес. после пилота) | 1 пилот Марафона + не более 1 платного follow-on | грант до ₽4 млн + follow-on ₽4–20 млн |

Мелкие ТСО в SAM не входят. Прогоны 50k/500k в README движка SynAPS —
другой домен (не постановка ТОиР); `feasibility_rate` оттуда на этот продукт
не переносится. Именованный макет района — 55 работ (РЭС «Северный»).
Генератор `medium`/`stress` (200/600 работ) после упаковки кампании: GREED
проходит проверку, FIFO — нет; сборка 600 работ — секунды. Это синтетический
фидер, не выгрузка ДЗО.

## Запрос

Пилот на площадке группы «Россети». KPI, которые можно считать на П0–П2:
время сборки графика (база «как есть» — у куратора ДЗО, из журнала заявок;
в репозитории этой цифры нет и выдумывать её нельзя), доля планов без
жёстких нарушений чекера, 0 сдвигов замороженных ПЛ. Не SAIDI и не N-1.
Грант до ₽4 млн закрывает **испытания** по статьям 410-ПП, не продажу
лабораторных 107. Нужны 10 позиций данных и куратор процесса ППР. Доступ в
SCADA не нужен.

## Что уже показано в репозитории

| Утверждение | Где смотреть |
| --- | --- |
| На синтетическом РЭС «Северный» GREED проходит проверку, FIFO — нет | `tests/test_res_severny.py`, `benchmark/results/jury_report.md` |
| CP-SAT доказывает оптимум makespan на той же постановке | `test_res_cpsat_proves_optimal_makespan` (маркер `slow`) |
| Перепланирование не двигает замороженные заявки ПЛ | Scenario B в том же контуре |
| Аварийные сутки (синтетика, регламент СТО 17330282 / приказ № 289: локализация→ремонт→опробование→ввод, ДГУ, замороженная ПЛ): GREED чист, FIFO 27 нарушений | `tests/test_emergency_day.py`, `benchmark/results/emergency_day_report.md` |
| Генератор 200/600 работ: GREED проходит проверку, FIFO ломает окна | `tests/test_scale_feeder.py`, `benchmark/results/scale_report.md` |
| Блок ГРЭС (синтетика): GREED чистый, FIFO нет | `tests/test_gres_block.py` |
| Подделка графика ловится | `tests/test_adversarial_*.py` |
| CLI exit 2 = fail-closed (`small --seed 42`) | README, `tests/test_cli_baselines.py` |
| Python и Rust сходятся по kind доменного слоя | `tests/test_native_parity.py` |
| Версия пакета = pin SynAPS в `pyproject.toml` | CLI `version`, `tests/test_version_pins.py` |
| Питч (22 слайда) | [`SynAPS-GridPlan.pdf`](SynAPS-GridPlan.pdf) |

РЭС «Северный» — типовая схема 110/35/10 кВ и открытые нормы длительностей
(СТО 34.01-24 и др.). Это не выгрузка ПАО «Россети».

В `jury_report.md` число жёстких нарушений равно сумме строк расшифровки
(правила GridPlan + движок SynAPS). GREED помечается «да» только при нуле
нарушений и `verified_feasible`.

## Вне скоупа

- N-1 и потокораспределение
- SAIDI/SAIFI как целевая функция
- живые данные ЭЛ5, Россети, Конаковской ГРЭС
- замена 1С:ТОИР / SCADA / OMS
- сертификат ISO 55000
- утверждение, что GREED оптимален
- ветвящиеся цепочки работ (join / fan-out); в движок уходит только линейная
  последовательность. На выгрузке ДЗО это всплывёт сразу — до пилота либо
  линеаризуем журнал, либо расширяем адаптер
- количество ЗИП на работу больше 1 (сейчас «позиция нужна / не нужна»)
- прогоны 50k операций движка как доказательство графика ТОиР
- численная база «часов сборки графика на РЭС» без данных куратора

Запрет «не отключать два аппарата сразу» — явное правило в постановке,
не анализ схемы сети. Показатель риска в отчёте — справочный, не целевая
функция.

## Уточнения

**Внедрено в Россети?** Нет. Синтетическая модель района.

**Почему TRL 4, если есть тесты и Rust?** Тесты на синтетике — лабораторный
макет. Релевантная среда по ISO 16290 — TRL 5–6.

**Это оптимизация надёжности сети?** Нет. Допустимый график ресурсов.

**Чем это не Excel и не 1С?** Excel не доказывает допустимость. 1С хранит
заявки и факт. Здесь считаются слоты и проверяются правила.

**Rust считает GREED?** Нет. Rust — FIFO и проверка. `cargo run -- check`
принимает JSON Python CLI (`id_map`).

**`solve --seed 42` вернул код 2?** Это fail-closed, не сломанный install.
GREED не моделирует эксклюзивность актива; чекер находит `ASSET_OVERLAP`.
Демо для жюри: `python benchmark/jury_benchmark.py`. На маленькой фикстуре
зелёный GREED — `--seed 12`.

**Оптимум на РЭС — значит оптимум везде?** CP-SAT доказал makespan на одной
синтетической постановке. Другие цели и размеры — отдельно.

**Это кибербезопасность или оборудование?** Нет. Планирование работ.

**Пригодно в аварийный день (особый режим)?** Синтетические аварийные сутки
узла «Восточный» по регламенту ликвидации (СТО 17330282.29.240.004-2008,
приказ Минэнерго № 289): локализация → осмотр → ремонт → опробование →
ввод в работу, оперативный персонал на переключениях, приоритет соцобъекту
(ДГУ на ФАП первой), возврат ТП с ДГУ на основную схему, хвост 0,4 кВ,
заявка ПЛ, согласованная накануне. Названия и топология вымышлены — это
не реконструкция живого филиала. GREED строит сутки без жёстких нарушений;
перепланирование после уточнения объёма по ВЛ-110 не двигает замороженную ПЛ.
См. `benchmark/emergency_day_benchmark.py` → `results/emergency_day_report.md`.

**Внедрено? Кто в команде?** Нет внедрения. Один автор репозиториев.
Эксперта ТОиР в git нет — если соглашение появится, ФИО только в анкете.

**Какая выручка?** Не считаем «рынок ПО». Bottom-up по контурам ДЗО.
Пилот = компенсация испытаний, не ARR.
