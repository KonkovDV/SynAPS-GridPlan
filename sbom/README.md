# SBOM

CycloneDX 1.5 inventories generated from `requirements-lock.txt` and `native/synaps-gridplan-rs/Cargo.lock` by `scripts/export_sbom.py`.

These files are a lockfile inventory for review. They are not a vulnerability scan, license legal opinion, or complete provenance of compiled native extensions.
