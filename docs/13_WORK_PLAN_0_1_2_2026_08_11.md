# SynAPS-GridPlan — work plan 0.1.2 (local only; not published)

Date: 2026-08-11
Scope: finish vertical slice (FIFO baseline, disruption diff, enriched reports,
fail-closed synthetic modes, reproducible FIFO+GREED benchmark).

## Academic / industry anchors (Aug 2026)

| Anchor | Relevance to GridPlan | Claim boundary |
| --- | --- | --- |
| MILP crew+restoration co-opt (IEEE / IOP 2025) | Crew routing + outage coupling is mainstream research | We ship schedule+checks, not network power-flow restore |
| Uncertainty-aware repair crew scheduling (2026) | Travel/duration uncertainty → rolling replan | Our disrupt/replan is deterministic synthetic, not stochastic |
| Dynamic crew allocation + damage assessment (IEEE TPWRS 2025) | Freeze / replan under new info | FrozenAssignment + IncrementalRepair only |
| LBBD energy-aware RCPSP (arXiv:2601.06542) | Validates SynAPS-style decomposition | Upstream SynAPS; GridPlan is domain contour |
| ČEZ Distribuce + Hexaly workforce | Real DSO field case | Industry analog ≠ our product |
| IBM Maximo Optimizer / Timefold | EAM + combinatorial scheduling | Competitors exist; RF niche = EAM without strong combo core |

Theory framing: maintenance outage planning ⊂ RCPSP with renewable resources
(crews), calendar windows (outages), consumables (ЗИП), and soft risk proxies.
Fail-closed verification separates **search** from **certificate of hard
feasibility** (SynAPS FeasibilityChecker + GridPlan post-checks).

## Delivered in 0.1.2

1. Calendar FIFO baseline (`baselines.py`) for honest synthetic comparisons.
2. Enriched `diff_plans` (late/unassigned/churn/metrics).
3. CLI disrupt emits machine-readable `diff`.
4. CSV/Markdown reports carry hashes, claim_level, provenance, frozen flags.
5. Synthetic modes `infeasible` / `frozen-conflict` for fail-closed demos.
6. Benchmark runner: FIFO + GREED (+ optional CPSAT), disruption scenario JSON.
7. Schema alignment: `frozen_assignments` on problem; `schedule_problem` required on result.
8. Tests for CLI round-trip, baselines, fail-closed modes.

## Explicit non-goals (do not claim)

- Deployment at Россети / Inter RAO
- ₽ savings, ENS reduction on live feeders
- SCADA/EAM/OMS integration
- УГТ 6+, “no analogs”, CVaR/RUL/VeriPB as product features
- Optimality certificates from GREED/FIFO

## Next 90 days (if pilot path opens)

1. Sanitized customer instance + dual-run vs current planner (human schedule).
2. Optional CPSAT-30 on small instances for gap tables (still experiment).
3. Travel matrix from GIS (adapter only; no GIS product claim).
4. Partner narrative: Россети ТОиР planning analytics — not robotics showcase.

## Verification checklist

- `python -m pip install -e ".[dev]"`
- `pytest -q`
- `python benchmark/run_benchmark.py`
- CLI: synthesize → solve FIFO/GREED → report → disrupt
