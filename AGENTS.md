# SynAPS-GridPlan

Energy-repair scheduling contour on SynAPS. Not the SynAPS kernel.

Pin: `pyproject.toml` / `src/synaps_gridplan/versions.py`.
Not N-1, not SAIDI, not 1С:ТОИР, not live DZO dumps.
`FEASIBLE` ⇒ `proven_hard_violations = ∅`.
ISO 16290 TRL 4. Synthetic fixtures. Not a plant pilot.
