# SynAPS-GridPlan — energy-repair scheduling (experiment)

Not MicroPhoenix. Not the SynAPS kernel. Never `C:\plans`.

Live roots: `C:\SynAPS`, `C:\SynAPS-GridPlan`, `C:\SynAPS-MobiRoute`, `C:\AeroBIM`.
Workspace: `C:\SynAPS\APS.code-workspace`.

Kernel pin: `pyproject.toml` / `versions.py`.
Not N-1, not SAIDI, not 1С:ТОИР, not live Россети / EL5.
`FEASIBLE` ⇒ `proven_hard_violations = ∅`.
